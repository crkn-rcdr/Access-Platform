import test from "ava";
import { tester } from "../common.spec";

import { Mets, validate } from "./Mets";

const { isValid, isInvalid } = tester(validate);

// test path is a string

// test md5 is a string

// test md5 matches pattern
