{
  "name": "access-platform-databases",
  "version": "1.0.0",
  "description": "Access Platform database schemas. This file is helpful for installing ajv-cli for schema testing.",
  "scripts": {
    "inspect": "kivik inspect Databases",
    "validate": "kivik validate",
    "test": "yarn exec -- mocha --recursive --timeout 99999 test/",
    "test:watch": "yarn exec wait-on http://couch:5984/ && yarn exec -- chokidar \"Databases/**/*.js\" \"spec/**/*.json\" \"test/**/*.js\" -c \"yarn run test\" --silent --initial",
    "docker:test": "docker-compose up --build"
  },
  "author": "Sascha Adler <sadler@crkn.ca>",
  "license": "MIT",
  "dependencies": {
    "chai": "^4.2.0",
    "chokidar-cli": "^2.1.0",
    "kivik": "crkn-rcdr/kivik",
    "mocha": "^8.1.3",
    "nano": "^8.2.2",
    "wait-on": "^5.2.0"
  }
}
