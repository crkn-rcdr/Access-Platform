{
  "$schema": "http://json-schema.org/draft-07/schema",
  "$id": "/accessObject.json",
  "title": "Generic object identifiable by a NOID",
  "type": "object",
  "oneOf": [{ "required": ["id"] }, { "required": ["_id"] }],
  "properties": {
    "_id": { "$ref": "noid.json" },
    "id": { "$ref": "noid.json" },
    "updateInternalmeta": {
      "description": "Trigger for updating the manifests that own this canvas in internalmeta, or result of an attempted update",
      "$ref": "update.json"
    }
  }
}
